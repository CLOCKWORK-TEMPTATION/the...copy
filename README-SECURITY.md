# 🔒 نظام الأمان التلقائي - Automated Security System

تم إعداد نظام فحص أمني شامل يعمل تلقائياً لحماية المشروع من تسريب المعلومات الحساسة.

## 🚀 البدء السريع

### 1. الإعداد الأولي
```bash
# إعداد git-secrets
./scripts/setup-git-secrets.sh

# اختبار النظام
./scripts/test-security-system.sh
```

### 2. الاستخدام اليومي
```bash
# الفحص يتم تلقائياً مع كل commit
git add .
git commit -m "your message"  # سيتم الفحص تلقائياً

# للفحص اليدوي
./scripts/security-scan.sh

# للفحص الشامل
./scripts/advanced-security-scan.sh
```

## 🛡️ مكونات النظام

### 1. Pre-commit Hook
- فحص تلقائي قبل كل commit
- يمنع تسريب الأسرار
- يفحص الملفات المُرحلة فقط

### 2. GitHub Actions
- فحص تلقائي مع كل push
- فحص أسبوعي مجدول
- تقارير أمنية شاملة

### 3. Scripts مخصصة
- `security-scan.sh` - فحص سريع
- `advanced-security-scan.sh` - فحص شامل
- `setup-git-secrets.sh` - إعداد الأدوات
- `test-security-system.sh` - اختبار النظام

## 🔍 ما يتم فحصه

### أسرار مكشوفة:
- كلمات المرور
- مفاتيح API
- رموز الوصول
- معلومات قواعد البيانات
- شهادات SSL

### ملفات حساسة:
- ملفات `.env`
- مفاتيح SSH
- شهادات SSL
- ملفات التكوين

### تبعيات:
- ثغرات أمنية
- إصدارات قديمة
- مكتبات غير آمنة

## 🚨 التعامل مع التحذيرات

### إذا تم اكتشاف مشكلة:
1. **راجع الرسالة** - اقرأ التحذير بعناية
2. **أصلح المشكلة** - أزل المعلومات الحساسة
3. **استخدم متغيرات البيئة** - ضع الأسرار في `.env`
4. **أعد المحاولة** - commit مرة أخرى

### في الحالات الطارئة فقط:
```bash
git commit --no-verify  # يتجاوز الفحص (غير مستحسن)
```

## 📊 تقارير الأمان

يتم إنشاء تقارير تلقائية تحتوي على:
- ملخص المشاكل المكتشفة
- توصيات للإصلاح
- إحصائيات الفحص
- خطة العمل

## 🔧 التخصيص

### إضافة نمط جديد:
```bash
git secrets --add 'your-pattern-here'
```

### إضافة استثناء:
```bash
git secrets --add --allowed 'allowed-pattern'
```

### تعديل الإعدادات:
عدّل الملفات:
- `.gitsecrets` - أنماط الفحص
- `scripts/security-scan.sh` - منطق الفحص
- `.github/workflows/security-scan.yml` - إعدادات CI/CD

## 📚 المزيد من المعلومات

- [دليل الأمان الكامل](docs/SECURITY.md)
- [GitHub Security Best Practices](https://docs.github.com/en/code-security)
- [OWASP Top 10](https://owasp.org/www-project-top-ten/)

---

**الأمان أولوية! 🔒**