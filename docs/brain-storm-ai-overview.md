# دليل صفحة العصف الذهني الذكي
## Brain Storm AI - Documentation

---

## 📋 نظرة عامة

### الوصف
صفحة **العصف الذهني الذكي** منصة متطورة متعددة الوكلاء للتطوير القصصي والإبداع الدرامي.

### الموقع
```
E:\the...copy\frontend\src\app\(main)\brain-storm-ai\
```

### الهدف
تمكين الكتّاب من تطوير أفكارهم الدرامية عبر:
- **27 وكيل ذكي متخصص**
- **5 مراحل منظمة**
- **نظام نقاش تفاعلي**
- **تحليل شامل**

---

## 🏗️ البنية المعمارية

### الملفات الأساسية

```
brain-storm-ai/
├── page.tsx                              # نقطة الدخول
└── brain-storm-content.tsx               # المكون الرئيسي (526 سطر)

lib/drama-analyst/
├── services/
│   ├── brainstormAgentRegistry.ts        # سجل الوكلاء (959 سطر)
│   └── geminiService.ts                  # خدمة Gemini AI (169 سطر)
├── orchestration/
│   └── multiAgentDebate.ts               # نظام النقاش (295 سطر)
├── core/
│   └── types.ts                          # الأنواع (296 سطر)
└── enums.ts                              # التعدادات (81 سطر)

components/
└── file-upload.tsx                       # رفع الملفات (307 سطر)
```

### التقنيات
- Next.js 15 + TypeScript
- React Hooks
- Tailwind CSS + Shadcn UI
- Google Gemini AI
- PDF.js + Mammoth.js

---

## 🤖 نظام الوكلاء (27 وكيل)

### التوزيع حسب الفئات

| الفئة | العدد | الوكلاء |
|------|------|---------|
| **Core** | 4 | مهندس النقد، مولّد المحاكاة، المنسق التركيبي، مواصل السرد |
| **Analysis** | 6 | محلل الإيقاع، محلل الشبكات، محلل البصمة الصوتية، منقّب المفاهيم، محلل البصمة الأدبية، محلل حقول التوتر |
| **Creative** | 4 | محوّل السياق، معمار المشاهد، مركّب الشخصيات، حدّاد الأكوان |
| **Predictive** | 4 | عرّاف الحبكة، محرك الدراما، مصفوفة التعاطف، محوّل المنصات |
| **Advanced** | 9 | مجهر النفسية، عدسة المحادثة، بصيرة السينما، منقّب الفلسفة، سياق الزمن، وحي الإنتاج، بوصلة الجمهور، قاضي الجماليات، مركّب الحكمة |

### قدرات الوكلاء

```typescript
interface BrainstormAgentCapabilities {
  canAnalyze: boolean;         // القدرة على التحليل
  canGenerate: boolean;        // القدرة على التوليد
  canPredict: boolean;         // القدرة على التنبؤ
  hasMemory: boolean;          // امتلاك ذاكرة
  usesSelfReflection: boolean; // التفكير الذاتي
  supportsRAG: boolean;        // دعم RAG
}
```

---

## 🎭 المراحل الخمس

### المرحلة 1: الملخص الإبداعي
- **الوكلاء:** 4 (مهندس النقد، محلل البصمة الأدبية، سياق الزمن، بوصلة الجمهور)
- **الهدف:** فهم الفكرة وتحليل السياق

### المرحلة 2: توليد الأفكار
- **الوكلاء:** 6 (مولّد المحاكاة، مواصل السرد، معمار المشاهد، مركّب الشخصيات، حدّاد الأكوان، عرّاف الحبكة)
- **الهدف:** توليد فكرتين متنافستين

### المرحلة 3: المراجعة المستقلة
- **الوكلاء:** 15 (جميع وكلاء التحليل والوحدات المتقدمة)
- **الهدف:** تحليل شامل من جميع الزوايا

### المرحلة 4: المناقشة التنافسية
- **الوكلاء:** 7 (الوكلاء الإبداعيون والتنبؤيون)
- **الهدف:** نقاش تفاعلي للوصول لأفضل نسخة

### المرحلة 5: القرار النهائي
- **الوكلاء:** 8 (المنسق التركيبي، مواصل السرد، مصفوفة التعاطف، محوّل المنصات، وحي الإنتاج، بوصلة الجمهور، قاضي الجماليات، مركّب الحكمة)
- **الهدف:** اتخاذ القرار وتقديم التوصيات

---

## 💬 نظام النقاش متعدد الوكلاء

### آلية العمل

```typescript
class MultiAgentDebateSystem {
  private maxRounds: number = 3;
  private consensusThreshold: number = 0.8;

  async conductDebate(
    task: string,
    context: any,
    participatingAgents: string[]
  ): Promise<DebateResult>
}
```

### المراحل

1. **الجولة الأولى:** مقترحات أولية من كل وكيل
2. **الجولات التالية:** تحسين المقترحات بناءً على الآخرين
3. **فحص التوافق:** حساب التشابه والثقة
4. **قرار الحكم:** اختيار أو دمج أفضل المقترحات

### النتيجة

```typescript
interface DebateResult {
  proposals: AgentProposal[];   // جميع المقترحات
  finalDecision: string;        // القرار النهائي
  consensus: boolean;           // هل تم التوافق؟
  debateRounds: number;         // عدد الجولات
  judgeReasoning: string;       // تفسير الحكم
}
```

---

## 🎨 المكونات الواجهية

### 1. page.tsx
```typescript
// Dynamic Import للأداء
const BrainStormContent = dynamic(() => import("./brain-storm-content"), {
  loading: () => <LoadingState />,
  ssr: false,
});
```

### 2. brain-storm-content.tsx
المكون الرئيسي يحتوي على:
- **لوحة التحكم:** اختيار المراحل وإدخال الفكرة
- **منطقة النقاش:** عرض المقترحات والقرارات
- **قائمة الوكلاء:** عرض الوكلاء وحالاتهم

### 3. file-upload.tsx
```typescript
interface FileUploadProps {
  onFileContent: (content: string, filename: string) => void;
  accept?: string;      // ".pdf,.docx,.txt"
  maxSize?: number;     // 10 MB
}
```

**الميزات:**
- السحب والإفلات
- استخراج النص من PDF/DOCX/TXT
- شريط التقدم
- معالجة الأخطاء

---

## 🔄 التدفق التشغيلي

### بدء جلسة

```typescript
handleStartSession()
  ↓
إنشاء Session جديدة
  ↓
تفعيل وكلاء المرحلة 1
  ↓
executeAgentDebate()
  ↓
multiAgentDebate.conductDebate()
  ↓
عرض النتائج
```

### الانتقال بين المراحل

```typescript
handleAdvancePhase()
  ↓
nextPhase = activePhase + 1
  ↓
تحديث Session
  ↓
executeAgentDebate(nextPhaseAgents)
  ↓
عرض النتائج الجديدة
```

---

## 🛠️ الخدمات المساعدة

### geminiService.ts

```typescript
class GeminiService {
  // توليد المحتوى
  async generateContent(prompt: string): Promise<string>
  
  // التحليل
  async analyze(request: AIRequest): Promise<AIResponse>
  
  // معلومات النموذج
  getModelInfo(): { model: string; maxTokens: number }
}
```

**الميزات:**
- إعادة المحاولة التلقائية (3 محاولات)
- Timeout للطلبات
- معالجة الأخطاء
- تنظيف المدخلات

---

## 📊 الأنواع والواجهات

### الأنواع الرئيسية

```typescript
// حالة الوكيل
interface AgentState {
  id: string;
  status: "idle" | "working" | "completed" | "error";
  lastMessage?: string;
  progress?: number;
}

// الجلسة
interface Session {
  id: string;
  brief: string;
  phase: BrainstormPhase;
  status: "active" | "completed" | "paused" | "error";
  startTime: Date;
  activeAgents: string[];
  results?: Record<string, unknown>;
}

// رسالة النقاش
interface DebateMessage {
  agentId: string;
  agentName: string;
  message: string;
  timestamp: Date;
  type: "proposal" | "critique" | "agreement" | "decision";
}
```

---

## 🔐 الأمان والأداء

### الأمان
- ✅ تنظيف المدخلات
- ✅ منع Format String Injection
- ✅ التحقق من الملفات (نوع وحجم)
- ✅ Timeout للطلبات

### الأداء
- ✅ Dynamic Import
- ✅ useMemo للتخزين المؤقت
- ✅ useCallback للدوال
- ✅ SSR معطل

---

## 📈 الإحصائيات

```typescript
interface AgentStats {
  total: 27
  byCategory: {
    core: 4,
    analysis: 6,
    creative: 4,
    predictive: 4,
    advanced: 9
  }
  withRAG: 20
  withSelfReflection: 18
  withMemory: 22
  averageComplexity: 0.85
}
```

---

## 🎯 حالات الاستخدام

1. **تطوير فكرة جديدة:** المرور بجميع المراحل الخمس
2. **تحليل سيناريو موجود:** رفع ملف → التحليل → التوصيات
3. **مقارنة فكرتين:** التوليد → النقاش → الاختيار

---

## 📝 ملاحظات التطوير

### الملفات المرتبطة
- `@/lib/drama-analyst/services/brainstormAgentRegistry.ts` - سجل الوكلاء الكامل
- `@/lib/drama-analyst/orchestration/multiAgentDebate.ts` - نظام النقاش
- `@/lib/drama-analyst/services/geminiService.ts` - خدمة AI
- `@/components/file-upload.tsx` - رفع الملفات
- `@/lib/drama-analyst/core/types.ts` - الأنواع
- `@/lib/drama-analyst/enums.ts` - التعدادات

### التبعيات الرئيسية
```json
{
  "next": "15.x",
  "react": "18.x",
  "@google/genai": "latest",
  "pdfjs-dist": "4.4.168",
  "mammoth": "latest",
  "lucide-react": "latest"
}
```
